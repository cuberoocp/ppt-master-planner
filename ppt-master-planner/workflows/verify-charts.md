# Verify Charts Workflow

> Run between executor and post-processing steps when the deck contains data charts.

## Process

1. Identify all chart SVG pages
2. Verify chart coordinate alignment (axis labels, data points, legends)
3. Calibrate colors and fonts to match spec_lock.md
4. Fix any chart-specific SVG issues
5. Confirm all data visualizations render correctly