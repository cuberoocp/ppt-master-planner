# Create Template Workflow

> Standalone template creation — no source deck required.

## Process

1. Define page roster (cover, toc, chapter, content, ending)
2. Define design spec (canvas, colors, typography, layout)
3. Generate template SVGs for each page type
4. Create `design_spec.md` and update `layouts_index.json`
5. Validate template structure