# Brand Identity Presets

Each brand directory contains:
- `brand.json` — color palette, typography, logo references, voice guidelines
- Logo files (PNG, SVG)

Brands are created via the `create-brand` workflow and applied via explicit user-provided directory path.